package com.pantuo.dynamic.service;

import org.springframework.stereotype.Service;

beans {
     xmlns([ctx:'http://www.springframework.org/schema/context'])

    footballMatch(HelloWorldService) {
        print "aa";
    }
}
